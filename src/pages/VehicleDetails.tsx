import React from 'react';
import { useParams } from 'react-router-dom';
import { useQuery } from '@tanstack/react-query';
import { 
  Truck, Calendar, Wrench, FileText, Fuel, AlertTriangle,
  MapPin, User, Clock, BarChart2
} from 'lucide-react';
// ... (rest of the file remains the same, just replace Tool with Wrench in the imports)